// legge coefficienti di due rette nel piano (y=mx+q) e stabilisce se sono parallele, incidenti o perpendicolari

#include <iostream>

using namespace std;

int main()
{
	/* acquisisci (m,q) delle due rette
	 * se m1==m2 allora sono parallele
	 * altrimenti sono incidenti
	 * inoltre, se (condizione di perpendicolarità)
	 *          allora sono perpendicolari
}
