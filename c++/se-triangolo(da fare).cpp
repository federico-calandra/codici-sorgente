// legge tre numeri reali e stabilisce se possono essere i lati di un triangolo, se sì ne determina il tipo
#include <iostream>

using namespace std;

int main()
{
	double l1, l2 l3;
	
	if(l1 == l2 && l2 = l3)
		cout << "sono lati di un triangolo equilatero";
		
	if(l1 == l2 && l2 != l3)
		
}
